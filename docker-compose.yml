services:
  uno:
    build: .
    image: ahmadmcer/uno-game-js:latest
    container_name: uno-game
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - UNO_PORT=3001
      - NODE_ENV=production
